#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
ll a,b,T;
string sum;

int main(){
	cin.tie(NULL);
	cout.tie(NULL);
	cin >> T;
	for (int i = 1; i <= T; i++){
		sum += "*";
		cout << sum << '\n';
	}
	
}